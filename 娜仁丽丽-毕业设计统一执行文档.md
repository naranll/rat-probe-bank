# 娜仁丽丽-毕业设计统一执行文档

> 用途说明：本文件是“娜仁丽丽”同学毕业设计后续修改、实现、实验和答辩准备的唯一统一文档。后续不再拆分为讨论纪要、章节稿、实现说明书等多个版本，一律以本文件为准。

---

## 0. 本文件怎么用

### 核心结论
这不是普通讨论记录，而是后续改中期报告、推进系统实现、组织实验和准备答辩的唯一执行稿。

### 使用要求
- 后续修改题目、选题简介、研究内容、创新点、模型选择、实验设计时，一律以本文件为准。
- 如果现有中期报告内容与本文件冲突，以本文件为准。
- 后续不再发散新方向，不再改成“木马本体检测”“普通验证码”“鼠标身份认证”等其他课题。
- 答辩准备时，优先背熟每节中的“核心结论”。

---

## 1. 课题最终定位

### 核心结论
本课题研究的不是木马程序本体的查杀，而是在Web高风险操作场景下，主动检测“当前操作是否处于远程真人操控状态”。

### 统一表述
- 本研究与远程控制木马风险密切相关。
- 但本文不做木马文件、恶意进程、恶意流量的检测。
- 本文关注的是远控手段在Web会话层面造成的后果，即当前操作是否表现出远程真人操控特征。
- 因此，本文可被表述为：`远程控制木马风险防护体系中的一个行为侧检测环节`。

### 正文建议写法
可以在选题背景或研究意义中这样表述：

“远程控制木马是形成远程真人操控风险的重要技术手段之一，因此本研究与远程控制木马风险具有直接关联。但本文关注的并不是木马样本本身的发现、分析与查杀，而是其在Web高风险业务场景中表现出的远程操控行为后果。换言之，本文研究的是远程控制行为检测问题，可作为远程控制木马风险防护体系中的行为侧补充环节。”

### 不能再写成
- 通用木马检测
- 普通验证码设计
- 鼠标行为身份认证
- 机器人/脚本识别

---

## 2. 题目最终确定

### 核心结论
唯一题目确定为：

`面向Web风控场景的主动挑战式远程真人操控实时检测方法研究`

### 题目解释
- `Web风控场景`：明确应用场景
- `主动挑战式`：明确方法路线
- `远程真人操控`：明确检测对象
- `实时检测方法`：明确研究目标

### 为什么这个题目不能再改散
- 一旦题目里没有“Web风控场景”，老师就会追问是不是终端安全、恶意代码分析或通用检测。
- 一旦题目里没有“主动挑战式”，老师就会看不出和普通被动行为分析的区别。
- 一旦题目里没有“远程真人操控”，老师就会误解成脚本检测、人机识别或身份认证。

---

## 3. 课题要解决的问题

### 核心结论
在Web高风险操作发生时，如何通过一次短时主动交互挑战，实时判断当前操作是否受到远程真人操控。

### 标准问题定义
在Web业务系统中，当用户执行转账、改密、授权确认等高风险操作时，系统如何在不依赖终端安装、不依赖流量解密、不显著增加用户负担的前提下，通过一次短时前端主动交互任务，实时检测当前操作是否具有远程真人操控特征。

### 这个问题和传统方案的区别
- 传统终端检测问的是：电脑里有没有木马程序。
- 传统流量检测问的是：网络里有没有恶意通信。
- 传统行为分析问的是：用户整体行为像不像异常用户。
- 本课题问的是：`就在这一次高风险Web操作里，当前控制行为像不像远程操控。`

---

## 4. 选题简介正式稿

### 核心结论
传统Web风控更擅长识别“异常登录”和“自动化攻击”，但对“合法环境下的远程真人操控”识别不足，因此需要一种主动、轻量、实时的检测机制。

随着Web业务系统不断承载转账、改密、授权确认、敏感信息提交等高风险操作，攻击者通过远程控制手段介入用户终端并实施会话内操控的风险持续上升。与传统异常登录、自动化脚本攻击不同，远程真人操控通常发生在合法账号、合法设备、合法网络环境下，且操作过程由真人完成，因而在表面上更接近正常用户行为。对于这类场景，传统依赖设备指纹、登录环境、地理位置以及历史行为画像的被动风控手段往往难以及时识别。

远程控制木马是形成此类风险的重要技术手段之一，因此本课题与远程控制木马风险具有直接关联。但本文关注的并不是木马样本本身的发现、分析与查杀，而是其在Web高风险业务场景中表现出的远程操控行为后果。换言之，本文研究的是远程控制行为检测问题，可作为远程控制木马风险防护体系中的行为侧补充环节。

现有研究更多关注自动化脚本检测、通用异常行为分析或者终端侧恶意程序检测，而对于“Web场景下远程真人操控”这一更隐蔽、更贴近真实业务的安全问题，仍缺乏一种能够在高风险操作发生前短时间内完成判别的轻量级方法。为此，本文提出一种主动挑战式检测思路：在敏感操作触发时，前端主动发起一个短时连续交互任务，通过显化本地操作与远程操控在闭环控制过程中的时延差异，实现对远程真人操控风险的实时识别。

本文选择“保球不撞墙”连续控制小游戏作为主动挑战载体。用户需在短时间内通过连续点击修正小球运动方向，使其避免触碰边界。系统在这一过程中采集小球状态、用户点击行为与风险变化过程，从中提取反映控制滞后、过冲、振荡和边界风险的结构化特征，并基于随机森林模型进行风险判别，从而形成一种兼顾实时性、轻量化和可解释性的Web风控检测方法。

---

## 5. 研究挑战

### 核心结论
本课题最大的挑战，不是系统能不能跑起来，而是如何设计一个能够稳定放大远程控制延迟、又不过度受个体熟练度影响的主动挑战任务。

对于Web高风险业务而言，远程真人操控是一类特殊而隐蔽的威胁。攻击者可能通过远程控制木马、远程桌面软件或其他远控技术，在受害者终端上持续操控浏览器会话。由于操作并非脚本自动执行，而是由真人完成，因此其宏观行为往往较接近正常用户，传统人机识别思路无法直接套用。与此同时，Web风控场景对响应时效要求较高，系统通常需要在数秒内完成风险判断，而不能依赖较长时间的历史行为积累。

在此背景下，本课题面临以下几个核心挑战。

第一，检测对象具有高度伪装性。远程操控者是真人而非机器人，因此其行为在整体节奏和交互模式上容易与正常用户相似。  
第二，观测窗口很短。系统需要在敏感操作发生时快速判断风险，不能依赖长时间行为监测。  
第三，部署条件受限。本文不依赖终端安装、不依赖流量解密，因此必须在Web前端层面构造能够显化差异的检测任务。  
第四，实验设计容易引入混淆变量。如果任务设计不合理，模型学到的可能是个体差异、熟练度差异或游戏经验差异，而不是远程控制本身带来的时延影响。  
第五，模型必须符合风控场景要求。除检测性能外，系统还要求模型具有较好的解释性、稳定性和轻量部署能力。

---

## 6. 研究目标

### 核心结论
本课题的目标不是证明小游戏本身有效，而是证明主动挑战机制能够显化远程操控伪迹，并支撑轻量、可解释的实时风险判别。

本课题的研究目标分为三个层次。

第一，机制目标。验证在Web高风险操作前主动发起短时连续控制任务，是否能够有效放大本地操作与远程真人操控之间的闭环控制差异。  
第二，方法目标。围绕挑战过程中产生的行为数据，提取反映控制滞后、风险停留、过冲和振荡的结构化特征，并构建能够区分本地操作与远程操控的风险判别方法。  
第三，系统目标。实现一个可嵌入Web业务流程的原型系统，完成挑战触发、过程采样、特征提取、风险判别和结果输出，为后续实际风控接入提供原型基础。

---

## 7. 总体技术路线

### 核心结论
本课题的总体技术路线固定为：高风险操作触发主动挑战 → 连续控制任务显化差异 → 提取结构化行为特征 → 随机森林完成风险判别。

### 逻辑链条
1. 在高风险操作前触发主动挑战任务。
2. 使用连续控制型小游戏放大远程控制链路中的时延与反馈滞后。
3. 采集用户控制行为和小球运动状态。
4. 提取反映控制滞后、过冲、振荡和边界风险的结构化特征。
5. 用随机森林进行轻量、可解释的风险判别。

### 这条思路为什么成立
- 本地操作时，感知、判断和动作反馈形成较紧密的控制闭环。
- 远程操作时，中间插入了远程显示、网络传输、指令回传等链路。
- 单次点击未必足以显化差异，但在连续控制任务中，这种滞后会不断累积。
- 累积后的表现就是：更晚修正、更长贴边、更容易过冲、更频繁振荡、碰撞风险更高。

---

## 8. 游戏方案最终确定

### 核心结论
主动挑战任务唯一确定为：`保球不撞墙`

### 为什么选这个任务
- 它不是一次性点击任务，而是持续闭环控制任务。
- 用户需要不断经历“看状态 - 做判断 - 点一下 - 等反馈 - 再修正”。
- 远程操控的延迟不会只影响一次点击，而会在这个闭环中持续累积。
- 因此，远程条件下更容易出现：
  - 修正滞后
  - 贴边运行
  - 过冲
  - 振荡
  - 更高的碰撞风险

### 任务机制
- 页面中央有一个持续运动的小球。
- 小球受到轻微随机扰动。
- 用户通过点击画布修正小球方向。
- 目标是在限定时间内不让小球碰到边界。

### 这不是普通验证码
这个任务的作用不是验证“是不是人”，而是验证“这个人是不是在远程链路下操作”。

### 为什么不用静态点选或排序点击
- 静态点选更容易测到认知搜索能力和熟练度。
- 顺序点击更容易受到记忆、预判和任务熟悉度影响。
- 连续控制任务更能稳定放大远程操控闭环延迟。
- 因此，`保球不撞墙`比离散点击方案更符合本课题的机制目标。

### 游戏设计原则
1. 规则必须一句话能讲清。
2. 挑战时间必须短，适合嵌入风控流程。
3. 过程必须连续，能放大时延积累。
4. 行为必须可量化，便于形成结构化特征。
5. 不应高度依赖游戏经验和复杂策略。

---

## 9. 研究内容最终结构

### 核心结论
全文研究内容固定为两部分，不再发散。

### 第一部分：主动挑战游戏设计与系统搭建
- 设计“保球不撞墙”连续控制任务
- 实现前端页面和交互逻辑
- 实现小球运动、点击修正和碰撞判定
- 实现日志采集与后端存储
- 形成完整实验平台

### 这一部分在论文里怎么写
建议统一写成“主动挑战机制设计与系统实现”，而不要写成“小游戏开发”。因为“小游戏开发”会显得工程味太重、研究味太弱，而“主动挑战机制设计”能突出这是为了显化远程控制差异而专门构造的实验环境。

### 第二部分：基于随机森林的风险判别模型构建
- 提取结构化行为特征
- 分析本地与远程条件下的差异
- 使用随机森林进行分类与风险判别
- 输出特征重要性，解释判别依据

### 这一部分在论文里怎么写
建议统一写成“风险特征构建与随机森林判别模型”，重点突出：
- 模型服务于机制验证
- 模型服务于风控解释
- 模型不是创新点本身

---

## 10. 创新点最终版本

### 核心结论
创新点要写机制，不要写“用了AI”。

### 创新点1
从被动风控转向主动挑战。

### 创新点2
从一般异常行为识别转向远程真人操控检测。

### 创新点3
利用闭环控制延迟伪迹进行检测，通过连续控制任务显化远程控制带来的滞后、过冲和振荡。

### 创新点4
结合轻量前端机制与可解释风险模型，具备Web风控场景中的工程落地潜力。

### 创新点写作提醒
- 不能把“用了随机森林”写成创新点。
- 不能把“用了机器学习”写成创新点。
- 不能把“做了一个小游戏”写成创新点。
- 创新点必须始终回到：主动挑战、远程真人操控、闭环控制延迟显化。

---

## 11. 工程量与已完成工作

### 核心结论
前期工作不是白做，而是后续重构主动挑战任务的工程底座。

### 可写成已完成工作
- 已完成基础Web前端原型与交互框架搭建
- 已完成前后端通信与数据落库链路
- 已完成基础行为采集能力
- 已完成实验流程跑通

### 中期后重点工作
- 将原有原型升级为“保球不撞墙”连续控制任务
- 新建标准化日志体系
- 重新采样本地/远程双条件数据
- 做特征提取与随机森林建模

### 这一段在中期报告里怎么写
建议写成“前期已完成基础原型与采集链路搭建，在此基础上识别出原有离散交互方案对远程闭环控制延迟的显化能力不足，因此中期后将研究重点收敛至连续控制型主动挑战任务，并围绕新的挑战任务开展数据采集与风险建模。”

---

## 12. 模型选择最终结论

### 核心结论
唯一核心模型确定为：`随机森林`

### 为什么选随机森林
1. 输入是结构化行为特征，不是图像、语音或超长原始时序。
2. 样本规模属于本科毕业设计常见的中小规模数据。
3. Web风控场景强调解释性，模型需要告诉我们“为什么判成高风险”。
4. 随机森林训练和推理成本适中，适合轻量化实现和工程部署。
5. 随机森林可以输出特征重要性，便于将模型结果和前面的机制假设对上。

## 12.1 为什么不用深度神经网络

### 核心结论
本课题不用深度神经网络，不是因为深度学习不先进，而是因为在当前课题的数据形态、样本规模和风控要求下，深度神经网络并不是最匹配的问题求解工具。

首先，模型选择必须服从研究对象。本课题处理的不是图像识别，不是语音识别，也不是海量文本建模，更不是长时间高频原始信号的端到端表示学习。课题的原始数据虽然来自小游戏交互过程，但最终并不是直接把整个像素序列、原始轨迹序列或者高维时序片段直接输入模型，而是围绕“远程控制导致闭环控制失稳”这一核心机理，提取出一组会话级结构化行为特征。这些特征包括危险区停留时间、首次修正延迟、过冲次数、振荡幅度、有效点击比例、风险下降效率等。换句话说，本课题最终要解决的是一个基于中小规模表格特征的监督分类问题。对于这种问题，深度神经网络并不天然占优。

其次，深度神经网络真正擅长的场景，通常满足两个前提：一是原始输入维度高、结构复杂，人工特征难以充分表达；二是数据规模足够大，可以支撑大量参数的稳定训练。而本课题恰恰不满足这两个条件。第一，本课题不是把完整原始高维轨迹直接端到端喂给模型，而是已经通过机制分析和特征工程，把最关键的风险信息压缩为结构化统计量。第二，本课题数据规模受本科毕业设计实验条件限制，不可能达到成千上万名被试、数十万条高质量远程操控样本的量级。在这种情况下，如果强行使用深度神经网络，很容易出现模型参数远大于有效样本信息量的情况，导致训练结果高度依赖样本划分和随机初始化，最终表现为训练集上结果好看、换一个被试就不稳定。

再次，深度神经网络的引入会模糊本课题真正的研究重点。本课题的核心创新点并不在分类器本身，而在于主动挑战机制是否能显化远程操控伪迹以及这些伪迹能否被构造成清晰的风险特征。也就是说，本课题首先是一个机制验证问题，其次才是一个分类建模问题。在这种情况下，如果一开始就把主线放到深度神经网络上，论文很容易变成“换个更复杂的模型去拟合数据”，从而削弱机制设计和特征解释的重要性。

最后，深度神经网络在风控场景中的一个天然弱点，就是解释成本较高。风控场景下，模型不仅要输出高风险或低风险，还要能回答“为什么判成高风险”。如果模型能够指出：本轮挑战中用户在危险区的停留时间显著偏长、危险后的首次修正明显滞后、修正后出现多次过冲和高频振荡，那么这样的结论更容易被业务侧理解，也更便于后续审核与策略调整。随机森林可以天然支持这类解释，而深度神经网络则难以在论文阶段提供同样直观的依据。

## 12.2 为什么随机森林更适合本课题

### 核心结论
随机森林之所以更适合本课题，是因为它在结构化特征、小样本条件、风控解释需求和工程实现约束之间取得了更好的平衡。

随机森林是由多棵决策树构成的集成学习模型，能够通过对样本和特征的随机采样建立多个子分类器，再用投票方式形成最终结果。对于本课题来说，这种模型有几个非常重要的优势。

第一，随机森林非常适合处理结构化表格特征。本课题最终输入模型的是会话级特征向量，而不是原始高维感知数据。随机森林对这类输入的适配性很强，不需要额外复杂的表示学习过程，就可以直接建模特征之间的非线性关系。

第二，随机森林对中小规模样本更友好。本课题的实验样本量在本科阶段已经算合理，但本质上仍属于中小规模数据。此时，深度神经网络往往会因为样本不足而表现不稳，而随机森林在这类数据条件下通常能保持更好的鲁棒性。

第三，随机森林更符合本课题的可解释性需求。随机森林可以直接输出特征重要性，从而帮助研究者回答两个关键问题：第一，哪些特征最能区分本地与远程操控；第二，这些特征是否与前面提出的理论机制一致。

第四，随机森林更符合风控系统的轻量化要求。训练和推理成本适中，部署门槛低，而且对计算资源要求相对温和。这使得它更适合作为本科毕业设计中的原型系统核心模型。

第五，随机森林更符合本课题“先验证机制，再验证效果”的研究节奏。对于一篇本科论文来说，最重要的是把“为什么这样设计”“这样设计是否成立”“模型为什么这样选”讲清楚。随机森林能够很好地承担这个角色：它足够有效，足够稳定，也足够容易解释。

### 统一答辩口径
本文不采用深度神经网络作为主模型，不是因为深度学习不先进，而是因为在当前任务形态、数据规模和风控场景要求下，随机森林更适合作为轻量、稳定、可解释的核心判别模型。

### 随机森林在答辩时的简化说法
“我们这个课题最终不是图像识别，也不是原始高维序列端到端建模，而是结构化行为特征分类问题。再加上风控场景特别强调解释性和轻量部署，所以我们最终选择了随机森林，而没有把深度神经网络作为主模型。”

---

## 13. 程序实现总目标

### 核心结论
系统实现的目标不是做一个好玩的网页小游戏，而是做一个稳定、可重复、能显化远程操控闭环滞后的主动挑战平台。

程序实现必须围绕以下总目标展开：
- 在Web页面中实现一个标准化的连续控制任务；
- 在任务过程中稳定记录用户行为和小球状态；
- 为后续提取结构化风险特征提供完整原始日志；
- 使系统可以支撑至少50名被试、每人多轮测试的正式实验。

---

## 14. 建议系统结构

### 核心结论
建议按“前端挑战页面 + 后端日志接口 + 离线特征提取与建模”三层结构实现，避免前后端职责混乱。

### 14.1 前端层
负责：
- 页面展示
- 小球运动更新
- 点击事件处理
- 实时状态记录
- 会话结束后日志打包上传

### 14.2 后端层
负责：
- 接收前端上传的原始日志
- 保存元数据与实验日志
- 提供会话编号和基本配置下发

### 14.3 离线分析层
负责：
- 原始日志清洗
- 会话级特征提取
- 统计分析
- 随机森林训练与验证

---

## 15. 前端页面设计

### 核心结论
前端页面必须简洁，所有多余视觉元素都应该删除，避免干扰被试控制行为。

建议页面只保留以下区域：
1. 标题区：显示当前任务名称，例如“风险验证挑战”
2. 状态区：显示被试编号、当前条件、当前轮次、剩余时间、是否已碰撞
3. 游戏画布区：固定为 `600px × 600px`
4. 控制按钮区：开始练习、开始正式实验、下一轮
5. 提示区：展示简短规则说明和每轮结束结果

不建议加入复杂动画、音效或额外计分元素，以免影响实验纯度。

---

## 16. 小球运动逻辑

### 核心结论
小球运动必须稳定、可控、可重复，不能依赖浏览器随机表现。

### 16.1 初始状态
- 小球半径：`12px`
- 初始位置：画布中心 `(300, 300)`
- 初始速度模长：`140 px/s`
- 初始方向：可固定一个角度，或在小范围内随机初始化

### 16.2 每帧更新
每一帧执行以下步骤：
1. 读取当前时间差 `dt`
2. 按当前速度更新位置
3. 判断是否进入一级危险区、二级危险区或发生碰撞
4. 若达到随机扰动触发时间，则对当前速度方向施加小角度偏转
5. 记录当前帧状态日志
6. 刷新画面显示

### 16.3 随机扰动设计
建议每 `350ms` 施加一次轻微扰动：
- 随机角度范围：`[-12°, +12°]`
- 扰动只改变方向，不直接改变速度模长

### 16.4 边界与危险区定义
设最近边界距离为：

`d = min(x - r, 600 - x - r, y - r, 600 - y - r)`

定义：
- 若 `d >= 80`：安全区
- 若 `40 <= d < 80`：一级危险区
- 若 `0 < d < 40`：二级危险区
- 若 `d <= 0`：碰撞

---

## 17. 点击修正机制

### 核心结论
点击修正必须简单一致，保证不同被试看到的是同一种控制规则。

### 17.1 推荐修正方式
每次点击时：
1. 计算点击位置相对小球当前位置的方向向量
2. 将方向向量归一化
3. 按固定强度叠加一个瞬时速度分量

其中修正强度固定为：
- `80 px/s`

### 17.2 修正后的速度处理
修正完成后，建议把速度模长重新归一到一个合理区间，例如：
- 最小速度：`100 px/s`
- 最大速度：`180 px/s`

### 17.3 不建议的交互方式
- 长按持续施力
- 键盘操作
- 拖拽操作

---

## 18. 前端状态机设计

### 核心结论
建议把游戏过程写成清晰的状态机，避免轮次切换、日志记录和结束逻辑混乱。

建议定义以下状态：
1. `idle`
2. `practice_ready`
3. `practice_running`
4. `formal_ready`
5. `formal_running`
6. `trial_end`
7. `session_end`

每次状态变化都建议记录到日志中，方便后期排查流程错误。

---

## 19. 日志记录设计

### 核心结论
日志设计必须从一开始就标准化，否则后面特征提取会非常痛苦。

### 19.1 按帧状态日志
- `session_id`
- `participant_id`
- `condition`
- `trial_id`
- `frame_index`
- `timestamp_ms`
- `ball_x`
- `ball_y`
- `ball_vx`
- `ball_vy`
- `ball_speed`
- `distance_left`
- `distance_right`
- `distance_top`
- `distance_bottom`
- `min_distance_to_wall`
- `danger_level`
- `risk_score`
- `perturbation_applied`
- `perturbation_angle`

### 19.2 点击事件日志
- `session_id`
- `participant_id`
- `condition`
- `trial_id`
- `click_id`
- `timestamp_ms`
- `click_x`
- `click_y`
- `ball_x_at_click`
- `ball_y_at_click`
- `ball_vx_before`
- `ball_vy_before`
- `ball_vx_after`
- `ball_vy_after`
- `min_distance_to_wall_at_click`
- `danger_level_at_click`

### 19.3 会话结果日志
- `session_id`
- `participant_id`
- `condition`
- `trial_id`
- `trial_type`
- `start_time`
- `end_time`
- `duration_ms`
- `collision_flag`
- `collision_side`
- `total_clicks`
- `danger1_duration_ms`
- `danger2_duration_ms`
- `avg_risk_score`
- `max_risk_score`

---

## 20. 后端接口设计建议

### 核心结论
后端不需要复杂，但必须能稳定收日志、分条件、按被试组织数据。

建议至少实现以下接口：

### 20.1 创建会话接口
`POST /api/session/create`

输入：
- `participant_id`
- `condition`
- `browser_info`
- `device_info`
- `screen_info`

输出：
- `session_id`
- `config_version`

### 20.2 上传单轮日志接口
`POST /api/session/uploadTrial`

输入：
- `session_id`
- `trial_id`
- `state_logs`
- `click_logs`
- `trial_summary`

### 20.3 会话结束接口
`POST /api/session/finish`

输入：
- `session_id`
- `participant_id`
- `condition`
- `trial_count`
- `valid_trial_count`

---

## 21. 文件组织建议

### 核心结论
原始数据、处理后数据和建模结果必须分层存放，避免后期混乱。

建议目录结构如下：

```text
data/
  raw/
    local/
      participant_001/
      participant_002/
    remote/
      participant_001/
      participant_002/
  processed/
    cleaned/
    features/
    summaries/
  results/
    stats/
    models/
    figures/
```

---

## 22. 详细实验设计

### 核心结论
实验必须证明的不是“谁更会玩游戏”，而是“远程条件下连续控制行为会出现更明显的控制失稳”。

### 22.1 实验目标
验证在相同任务、相同被试条件下，仅由于远程控制链路存在，用户在连续控制小游戏中的行为是否会出现显著不同的控制特征。

### 22.2 样本规模最终要求
- 测试人数至少：`50人`
- 每名被试都必须完成多轮测试
- 每名被试都做本地与远程两种条件

### 为什么至少50人
1. 12人或20人虽然能做初步实验，但对毕业设计后期的结果稳定性和说服力仍偏弱。
2. 50人能够更好覆盖个体差异，降低“某几个被试行为特别异常”对整体结论的影响。
3. 本课题强调的是跨被试稳定规律，而不是在少数人身上碰巧成立，因此样本量必须明显上一个台阶。
4. 如果最终能做到50人以上，整篇论文在实验设计上会更扎实，答辩时也更有底气。

### 22.3 实验任务
- 任务名称：`保球不撞墙`
- 画布中有持续运动的小球
- 小球不断受到轻微随机扰动
- 用户通过点击修正小球方向
- 目标是在6秒内不碰到边界
- 碰撞即结束

### 22.4 固定参数
- 画布尺寸：`600 x 600`
- 小球半径：`12 px`
- 初始位置：`(300, 300)`
- 基础速度：`140 px/s`
- 更新方式：`requestAnimationFrame`
- 状态采样频率：约`60Hz`
- 扰动间隔：每`350ms`
- 扰动角度：`[-12°, +12°]`
- 点击修正强度：叠加`80 px/s`方向分量
- 一级危险区：距边界小于`80px`
- 二级危险区：距边界小于`40px`
- 单轮时长：`6秒`

### 22.5 被试设计
- 被试人数最终目标：`至少50人`
- 每名被试都做两种条件：
  - `local`：直接在本机操作
  - `remote`：被试本人通过远程桌面连接同一台电脑操作
- 远程条件下不能由研究者代替操作
- 远程工具固定一种，建议：`AnyDesk`

### 被试招募建议
- 优先在学院、实验室、同专业同学中招募。
- 每位被试单次完整实验时间控制在20到30分钟内。
- 若50人一次性难以完成，可分批次采集，但所有批次必须使用同一版本系统、同一参数和同一指导语。

### 22.6 轮次安排
每名被试在每种条件下完成：
- `3轮练习`
- `30轮正式实验`

即每人总正式轮数：
- 本地30轮
- 远程30轮
- 合计60轮

### 为什么要多轮测试
1. 单轮行为波动很大，不能代表稳定控制能力。
2. 远程延迟效应可能在部分轮次被掩盖，因此需要多轮采样来平均偶然性。
3. 多轮测试可以帮助提取会话级稳定特征，而不是只看单轮成败。
4. 多轮测试还能让后续留一被试交叉验证更稳。

### 22.7 顺序控制
- 一半被试先做本地再做远程
- 另一半被试先做远程再做本地

### 22.8 统一指导语
建议统一使用：

“请在每轮挑战中通过连续点击控制小球运动，尽量避免其碰到边界。每轮持续6秒，碰撞即结束。请尽量自然操作，不要故意放慢或加快节奏，也不要刻意尝试特殊策略。”

### 22.9 原始数据记录

#### A. 按帧状态日志
- `session_id`
- `participant_id`
- `condition`
- `trial_id`
- `timestamp_ms`
- `ball_x`
- `ball_y`
- `ball_vx`
- `ball_vy`
- `ball_speed`
- `distance_to_left`
- `distance_to_right`
- `distance_to_top`
- `distance_to_bottom`
- `min_distance_to_wall`
- `danger_level`
- `random_perturbation_applied`
- `perturbation_angle`

#### B. 点击事件日志
- `session_id`
- `participant_id`
- `condition`
- `trial_id`
- `click_id`
- `timestamp_ms`
- `click_x`
- `click_y`
- `ball_x_at_click`
- `ball_y_at_click`
- `ball_vx_before`
- `ball_vy_before`
- `ball_vx_after`
- `ball_vy_after`
- `distance_to_wall_at_click`
- `danger_level_at_click`

#### C. 会话结果日志
- `session_id`
- `participant_id`
- `condition`
- `trial_id`
- `start_time`
- `end_time`
- `survival_time_ms`
- `collision_flag`
- `collision_wall`
- `total_clicks`
- `total_frames`
- `danger1_duration_ms`
- `danger2_duration_ms`
- `max_risk_score`
- `avg_risk_score`

### 22.10 数据组织建议
- 一名被试对应一个`participant_id`
- 每次实验条件对应一个`session_id`
- 每一轮对应一个`trial_id`

### 22.11 风险过程定义
定义帧级风险值：
- 安全区：`risk = 0`
- 一级危险区：`risk = 1`
- 二级危险区：`risk = 2`
- 若处于危险区且连续`300ms`无点击：额外`+1`

这个中间量用于后续计算：
- 平均风险
- 最大风险
- 风险积分面积
- 风险停留时长

### 22.12 特征提取

#### A. 边界风险特征
- 最近边界距离均值
- 最近边界距离最小值
- 一级危险区停留比例
- 二级危险区停留比例
- 进入危险区次数
- 是否碰撞
- 存活时长

#### B. 反应滞后特征
定义“危险事件”为小球首次进入一级危险区。

提取：
- 进入危险区到第一次点击的时间
- 危险状态下前两次点击间隔
- 从进入危险区到重新回到安全区所需时间

再做会话级聚合：
- 均值
- 标准差
- 最大值

#### C. 过冲特征
- 过冲次数
- 平均过冲幅度
- 最大过冲幅度

#### D. 振荡特征
- 方向切换次数
- 高频修正次数
- 点击突发次数
- 边界侧切换次数
- 航向变化方差

#### E. 控制效率特征
- 每秒点击次数
- 有效点击比例
- 无效点击比例
- 单次点击平均风险下降量

### 建议的特征数量控制
- 第一版先控制在`25维左右`
- 不能一开始堆到五六十维，否则在论文阶段反而不稳
- 特征必须优先围绕机制，而不是追求数量

### 22.13 数据清洗规则
- 删除所有练习轮
- 页面失焦或切换窗口的轮次删除
- 帧数据缺失超过`10%`的轮次删除
- 总点击数为`0`且存活不足`1秒`的轮次删除
- 帧间隔大于`200ms`且超过`3次`的轮次删除
- 所有删除必须记录原因

### 额外清洗建议
- 如果某位被试远程连接明显中断，整段session作废并重采。
- 如果某轮开始前页面未正确重置小球状态，该轮作废。
- 如果出现系统版本变更，则不同版本数据不能混采进同一主实验结果。

### 22.14 统计分析
先做机制验证，再做分类。

比较本地与远程条件下以下关键特征：
- 危险响应延迟均值
- 二级危险区停留比例
- 过冲次数
- 有效点击比例
- 风险下降效率
- 存活时长

统计方法：
- 若近似正态：配对t检验
- 若非正态：Wilcoxon符号秩检验
- 同时报效应量

### 22.15 随机森林训练
- 输入：每轮实验对应的特征向量
- 标签：`0 = local`，`1 = remote`

主验证方式必须采用：
`留一被试交叉验证（Leave-One-Subject-Out CV）`

做法：
1. 每次留出1名被试全部样本作为测试集
2. 其余被试样本作为训练集
3. 重复直到每名被试都被测试一次
4. 汇总平均结果

评价指标：
- Accuracy
- Precision
- Recall
- F1
- AUC
- FAR
- FRR

### 22.16 特征重要性分析
随机森林训练完成后，必须输出特征重要性排序，用于回答：
- 哪些特征最能反映远程操控风险
- 模型判别依据是否与前面提出的机制假设一致

### 22.17 消融实验

#### 消融1：任务机制消融
比较：
- 原有离散点击任务
- 当前连续控制任务

目的：
证明为什么要从原方案收敛到“保球不撞墙”。

#### 消融2：特征类别消融
分别只使用：
- 边界风险特征
- 反应滞后特征
- 过冲振荡特征
- 全特征

目的：
分析哪类特征对识别远程操控贡献最大。

---

## 23. 预测试与正式实验安排

### 核心结论
先做小规模预测试锁版本，再做50人正式实验，不能边采正式数据边改系统。

### 预测试建议
- 先找5到8名同学做预测试
- 目的不是出最终结果，而是检查：
  - 游戏难度是否合适
  - 参数是否太难或太简单
  - 日志是否完整
  - 远程连接是否稳定
  - 数据清洗规则是否需要补充
- 预测试通过后，锁定版本，再做50人正式实验

---

## 24. 后续执行顺序

### 核心结论
后面工作不能乱，必须按照“任务成立 → 实验成立 → 特征成立 → 模型成立”的顺序推进。

### 顺序
1. 完成“保球不撞墙”正式版本
2. 完成标准化日志记录
3. 先做小规模预测试，确认系统稳定
4. 再按统一流程采集至少50名被试的本地/远程双条件数据
5. 做数据清洗与特征提取
6. 先做机制验证统计分析
7. 再做随机森林训练与验证
8. 输出特征重要性分析
9. 补做必要消融实验
10. 最后再整理论文和答辩材料

---

## 25. 答辩时必须统一的几个说法

### 核心结论
答辩时最重要的是口径统一，不要临场把题目讲散。

### 说法1：这是不是木马检测
不是木马程序本体检测，而是远程控制木马风险防护体系中的行为侧检测环节，检测的是Web会话中是否出现远程真人操控特征。

### 说法2：为什么不是普通验证码
因为本任务不是为了验证“是不是人”，而是为了通过连续控制过程显化“是不是在远程链路下操作”。

### 说法3：为什么不用深度学习
因为本文是结构化行为特征分类问题，样本规模有限，风控场景又强调解释性和轻量部署，因此随机森林比深度神经网络更适合当前课题。

### 说法4：为什么要50人多轮测试
因为本课题最怕把个体差异误当成远程操控差异，扩大被试规模并采用多轮测试，才能更稳定地提取跨被试规律。

---

## 26. 一句总纲

### 核心结论
整篇毕业设计必须始终围绕这一句展开：

**我们通过主动挑战显化远程控制伪迹，并用随机森林实现Web会话层的轻量、可解释风险检测。**

---

## 27. 原PDF逐段修改说明

### 核心结论
修改原PDF时，不能只做润色，必须按“保留什么、删除什么、重写什么”的方式整体换主线。

这一部分的作用，是直接告诉学生：她现在那份中期报告哪些内容还能留，哪些内容必须删掉，哪些内容要按新的课题逻辑重写。这里不是抽象建议，而是具体修改动作说明。

### 27.1 封面与题目页

#### 修改要求
- 原题目直接替换为：`面向Web风控场景的主动挑战式远程真人操控实时检测方法研究`
- 其余学院、专业、姓名、指导教师信息保持不变

#### 为什么这么改
原题目如果过于泛化，就会让老师误解为通用行为识别、通用木马检测或一般验证码研究。题目必须第一眼就把场景、方法和对象说清楚。

### 27.2 目录页

#### 修改要求
目录应调整为围绕以下结构组织：

1. 论文选题简介  
2. 已完成的工作  
3. 模型选择与技术路线  
4. 实验设计与后续工作安排  
5. 参考文献

如果原目录中有过于分散的章节命名，例如过度强调“通用鼠标行为分析”“一般人机识别”“离散点击验证机制”等，应在目录层面同步删除或改名。

### 27.3 第一章“研究背景/选题简介”

#### 处理建议
`改写，不建议只修补`

#### 原文中可保留的内容
- 对Web高风险场景的描述
- 对远程控制木马/远程控制风险的背景铺垫
- 对传统被动风控不足的说明

#### 原文中要删除或弱化的内容
- 任何把课题直接写成“木马检测方法”的表述
- 任何会把研究重心引向恶意代码本体分析、恶意流量解密或终端查杀的表述
- 任何把研究写成一般身份认证或人机识别的表述

#### 应重写成什么
第一章必须重写成：
- 本课题不做木马本体检测，而做Web会话层远程操控检测
- 传统风控在合法环境下的远程真人操控场景中识别不足
- 本文通过主动挑战式短时任务显化远程闭环控制差异
- 本文采用连续控制小游戏和随机森林模型构建风险判别方法

### 27.4 “研究意义与目标”部分

#### 处理建议
`重写`

#### 常见问题
原文如果写成“提高木马检测精度”“构建高精度检测算法”“实现复杂智能识别模型”，都容易把题目带偏。

#### 应重写成什么
研究意义应分成：
- 理论意义：把被动风控推进到主动挑战显影
- 应用意义：为Web高风险场景提供前端轻量化远程操控检测环节

研究目标应分成：
- 机制目标：证明主动挑战能显化远程控制差异
- 方法目标：构建结构化特征与随机森林判别方法
- 系统目标：实现Web原型系统并完成实验验证

### 27.5 “主要研究内容”部分

#### 处理建议
`整体改成两条主线`

#### 原文中应该删除的写法
- 过多分散的小点
- 把模型、系统、特征、游戏写成彼此平行又缺少主从关系
- 把“小游戏”本身当成主要研究目标

#### 应重构为
研究内容只保留两部分：

1. 主动挑战游戏设计与系统搭建  
2. 基于随机森林的风险判别模型构建

这两部分必须贯穿全文，不能再引出第三条、第四条平行主线。

### 27.6 “已完成的工作”部分

#### 处理建议
`保留基础，重写表述`

#### 原则
前期已经做过的东西不要全部否定，而是统一改写成“基础原型和采集链路已完成”。

#### 可保留内容
- 已有前端原型
- 已有数据采集基础
- 已有前后端链路
- 已有初步实验跑通

#### 应新增的表述
- 前期探索表明离散点击任务对远程闭环控制延迟显化能力不足
- 因此中期后研究重点收敛为连续控制型主动挑战任务

这样就能把“前期做过的事”自然转成“现在的新方向底座”，而不是显得前面全错了。

### 27.7 “模型与算法”部分

#### 处理建议
`大改，统一收敛到随机森林`

#### 必须删掉的写法
- 深度神经网络作为主模型的表述
- 多模型并列推进的表述
- “为了显得先进而引入AI/Agent/深度学习”的表述

#### 必须保留并强化的内容
- 为什么是结构化特征分类问题
- 为什么样本规模不适合深度学习作为主模型
- 为什么风控强调可解释性
- 为什么随机森林是轻量、稳定、可解释的合理选择

#### 最终写法要求
这一部分要明确：
- 唯一核心模型：随机森林
- 不把深度神经网络作为主模型的原因必须展开写
- 如果提到别的模型，也只能作为可能的补充对比，不进入正文主线

### 27.8 “实验设计”部分

#### 处理建议
`彻底重写`

#### 原文若存在以下问题，必须全部修正
- 被试人数过少
- 轮次太少
- 远程条件下不是同一个被试本人操作
- 没有顺序平衡设计
- 没有固定参数
- 没有数据清洗规则
- 没有留一被试验证

#### 新实验设计必须写成
- 至少50名被试
- 每人本地30轮、远程30轮正式实验
- 每种条件前3轮练习
- 远程条件由同一被试本人远程操作
- 采用顺序平衡设计
- 固定任务参数
- 固定数据清洗规则
- 先做机制验证，再做随机森林分类
- 主验证方式为留一被试交叉验证

### 27.9 “后续工作安排”部分

#### 处理建议
`改成清晰执行顺序`

#### 不要再写成
- 笼统的“继续完善系统”
- 笼统的“进一步优化模型”
- 笼统的“补充实验”

#### 应写成什么
1. 完成正式版小游戏  
2. 做预测试并锁定版本  
3. 做50人正式实验  
4. 做数据清洗与特征提取  
5. 做机制验证统计分析  
6. 做随机森林训练与验证  
7. 做特征重要性与消融实验  
8. 完成论文和答辩材料

### 27.10 一句话修改原则

#### 核心结论
凡是会把课题带向“木马本体检测、普通验证码、通用身份认证、深度学习炫技”的内容，都应该删；凡是能服务于“主动挑战显化远程操控差异”的内容，都应该保留和强化。

---

## 28. 小游戏实现代码骨架与数据结构设计

### 核心结论
代码实现先追求结构清晰、日志完整、可复现，不追求花哨界面和复杂工程封装。

这一部分给出的是一个适合当前课题阶段的实现骨架，重点是把系统做成能稳定采数据的实验平台，而不是做成产品级大工程。

### 28.1 推荐前端技术栈

#### 建议
- 前端：`React + TypeScript`
- 画布：`HTML5 Canvas`
- 状态更新：`requestAnimationFrame`
- 网络请求：`fetch` 或 `axios`

#### 原因
- React便于组织页面和状态
- TypeScript便于约束日志字段
- Canvas适合实时绘制运动小球

### 28.2 推荐前端目录结构

```text
src/
  components/
    ChallengeCanvas.tsx
    StatusPanel.tsx
    ControlPanel.tsx
  hooks/
    useChallengeEngine.ts
    useTrialLogger.ts
  types/
    challenge.ts
    logs.ts
  services/
    api.ts
  utils/
    math.ts
    risk.ts
  pages/
    ChallengePage.tsx
```

### 28.3 核心数据类型设计

#### 小球状态
```ts
export interface BallState {
  x: number;
  y: number;
  vx: number;
  vy: number;
  speed: number;
}
```

#### 单帧状态日志
```ts
export interface FrameLog {
  sessionId: string;
  participantId: string;
  condition: "local" | "remote";
  trialId: number;
  frameIndex: number;
  timestampMs: number;
  ballX: number;
  ballY: number;
  ballVx: number;
  ballVy: number;
  ballSpeed: number;
  distanceLeft: number;
  distanceRight: number;
  distanceTop: number;
  distanceBottom: number;
  minDistanceToWall: number;
  dangerLevel: 0 | 1 | 2;
  riskScore: number;
  perturbationApplied: boolean;
  perturbationAngle: number;
}
```

#### 点击日志
```ts
export interface ClickLog {
  sessionId: string;
  participantId: string;
  condition: "local" | "remote";
  trialId: number;
  clickId: number;
  timestampMs: number;
  clickX: number;
  clickY: number;
  ballXAtClick: number;
  ballYAtClick: number;
  ballVxBefore: number;
  ballVyBefore: number;
  ballVxAfter: number;
  ballVyAfter: number;
  minDistanceToWallAtClick: number;
  dangerLevelAtClick: 0 | 1 | 2;
}
```

#### 单轮结果日志
```ts
export interface TrialSummary {
  sessionId: string;
  participantId: string;
  condition: "local" | "remote";
  trialId: number;
  trialType: "practice" | "formal";
  startTime: number;
  endTime: number;
  durationMs: number;
  collisionFlag: boolean;
  collisionSide: "left" | "right" | "top" | "bottom" | "none";
  totalClicks: number;
  totalFrames: number;
  danger1DurationMs: number;
  danger2DurationMs: number;
  avgRiskScore: number;
  maxRiskScore: number;
}
```

### 28.4 游戏引擎Hook骨架

```ts
export function useChallengeEngine(config: ChallengeConfig) {
  const [ball, setBall] = useState<BallState>(createInitialBall(config));
  const [running, setRunning] = useState(false);
  const [trialEnded, setTrialEnded] = useState(false);

  const frameLogsRef = useRef<FrameLog[]>([]);
  const clickLogsRef = useRef<ClickLog[]>([]);
  const frameIndexRef = useRef(0);
  const lastPerturbationRef = useRef(0);
  const startTimeRef = useRef(0);

  const startTrial = () => {
    resetAllState();
    startTimeRef.current = performance.now();
    setRunning(true);
  };

  const handleClick = (x: number, y: number) => {
    if (!running) return;
    const updated = applyClickCorrection(ball, x, y, config);
    appendClickLog(ball, updated, x, y);
    setBall(updated);
  };

  const tick = (now: number) => {
    if (!running) return;
    const next = updateBall(ball, now, config);
    const risk = computeRisk(next, config);
    appendFrameLog(next, risk, now);

    if (isCollision(next, config)) {
      finishTrial(now, next);
      return;
    }

    if (now - startTimeRef.current >= config.trialDurationMs) {
      finishTrial(now, next);
      return;
    }

    setBall(next);
    requestAnimationFrame(tick);
  };

  return {
    ball,
    running,
    trialEnded,
    startTrial,
    handleClick,
  };
}
```

### 28.5 风险计算函数骨架

```ts
export function computeRisk(ball: BallState, config: ChallengeConfig): number {
  const d = getMinDistanceToWall(ball, config.canvasSize, config.ballRadius);

  if (d >= 80) return 0;
  if (d >= 40) return 1;
  if (d > 0) return 2;
  return 3;
}
```

如果需要实现“危险区内300ms无点击额外加1”，可以在Hook中结合最近一次点击时间进一步修正。

### 28.6 点击修正函数骨架

```ts
export function applyClickCorrection(
  ball: BallState,
  clickX: number,
  clickY: number,
  config: ChallengeConfig
): BallState {
  const dx = clickX - ball.x;
  const dy = clickY - ball.y;
  const len = Math.sqrt(dx * dx + dy * dy) || 1;

  const ux = dx / len;
  const uy = dy / len;

  let vx = ball.vx + config.clickBoost * ux;
  let vy = ball.vy + config.clickBoost * uy;

  const speed = Math.sqrt(vx * vx + vy * vy);
  const clamped = clamp(speed, config.minSpeed, config.maxSpeed);
  vx = (vx / speed) * clamped;
  vy = (vy / speed) * clamped;

  return {
    x: ball.x,
    y: ball.y,
    vx,
    vy,
    speed: clamped,
  };
}
```

### 28.7 前端页面骨架

```tsx
export default function ChallengePage() {
  const { ball, running, trialEnded, startTrial, handleClick } =
    useChallengeEngine(defaultConfig);

  return (
    <div className="challenge-page">
      <StatusPanel />
      <ChallengeCanvas ball={ball} onClickCanvas={handleClick} />
      <ControlPanel onStart={startTrial} running={running} />
      {trialEnded && <div>本轮结束</div>}
    </div>
  );
}
```

### 28.8 后端数据表建议

#### sessions表
字段：
- `session_id`
- `participant_id`
- `condition`
- `browser_info`
- `device_info`
- `screen_info`
- `created_at`

#### frame_logs表
字段与`FrameLog`一致，外加主键ID。

#### click_logs表
字段与`ClickLog`一致，外加主键ID。

#### trial_summary表
字段与`TrialSummary`一致，外加主键ID。

### 28.9 Python特征提取脚本建议

推荐单独写一个脚本，例如：

`extract_features.py`

输入：
- 原始frame_logs
- 原始click_logs
- trial_summary

输出：
- 每轮一行的`features.csv`

建议输出格式：

```csv
participant_id,condition,trial_id,danger_response_latency_mean,danger2_duration_ratio,overshoot_count,effective_click_ratio,click_rate,survival_time_ms,label
```

### 28.10 随机森林训练脚本建议

建议单独写：

`train_random_forest.py`

主要流程：
1. 读取`features.csv`
2. 按`participant_id`做留一被试交叉验证
3. 每轮记录Accuracy、Precision、Recall、F1、AUC
4. 汇总平均结果
5. 输出特征重要性

可用伪代码：

```python
participants = df["participant_id"].unique()
all_metrics = []

for pid in participants:
    train_df = df[df["participant_id"] != pid]
    test_df = df[df["participant_id"] == pid]

    X_train = train_df[feature_cols]
    y_train = train_df["label"]
    X_test = test_df[feature_cols]
    y_test = test_df["label"]

    model = RandomForestClassifier(
        n_estimators=300,
        max_depth=None,
        random_state=42,
        class_weight="balanced"
    )
    model.fit(X_train, y_train)
    y_pred = model.predict(X_test)
    y_prob = model.predict_proba(X_test)[:, 1]

    all_metrics.append(calc_metrics(y_test, y_pred, y_prob))
```

### 28.11 实现优先级建议

#### 第一优先级
- 前端游戏可稳定运行
- 日志可完整记录
- 单轮结果能正确结束并上传

#### 第二优先级
- 原始日志可成功落库
- 能导出并清洗数据
- 能稳定提取基础特征

#### 第三优先级
- 随机森林训练与验证跑通
- 特征重要性输出
- 补充消融实验

#### 不要一开始就做的事
- 不要先追求UI美化
- 不要先做复杂用户系统
- 不要先做深度学习模型
- 不要先做复杂在线推理服务

### 28.12 一句话实现原则

#### 核心结论
代码实现阶段最重要的是：系统稳定、日志完整、实验可复现、数据能支撑随机森林建模，而不是做出复杂炫目的软件工程结构。
